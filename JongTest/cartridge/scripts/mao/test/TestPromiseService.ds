/**
* Demandware Script File
* To define input and output parameters, create entries of the form:
*
* @<paramUsageType> <paramName> : <paramDataType> [<paramComment>]
*
* where
*   <paramUsageType> can be either 'input' or 'output'
*   <paramName> can be any valid parameter name
*   <paramDataType> identifies the type of the parameter
*   <paramComment> is an optional comment
*
* For example:
*
*-   @input ExampleIn : String This is a sample comment.
*-   @output ExampleOut : Number
*
*/
importPackage( dw.system );
importPackage( dw.util );
importPackage( dw.object );

function execute( pdict : PipelineDictionary ) : Number
{
    var promiseService = require('int_pacsun_api/cartridge/scripts/mao/service/PromiseService');

    var orderNo = "DW_0001";
    var shipToAddress = {};
    shipToAddress.address1 = "3450 E Miraloma Ave";
    shipToAddress.address2 = "";
    shipToAddress.firstName = "DMTEST";
    shipToAddress.lastName = "ACCEPT5";
    shipToAddress.city = "Anaheim";
    shipToAddress.postalCode = "92806";
    shipToAddress.state = "CA";
    shipToAddress.country = "US";
    shipToAddress.email = "jkim@pacsun.com";
    var productLineItems = new Array();

    var lineItem = {
        "ItemId": "0097514810014",
        "Quantity": 1,
        "OrderLineId": "1",
        "DeliveryMethodId": "ShipToAddress",
        "ShippingMethodId": "Standard"
    }
    productLineItems.push(lineItem);

    var lineItem = {
        "ItemId": "0097514810077",
        "Quantity": 2,
        "OrderLineId": "2",
        "DeliveryMethodId": "ShipToAddress",
        "ShippingMethodId": "Standard"
    }
    productLineItems.push(lineItem);

    var status = promiseService.promiseRequest(orderNo, shipToAddress, productLineItems);
    status = promiseService.promiseRequest(orderNo, shipToAddress, productLineItems);
    
   return PIPELET_NEXT;
}
